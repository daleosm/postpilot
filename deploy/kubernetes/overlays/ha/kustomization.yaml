apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: api-runtime.yaml

# The production ingress is intentionally an example rather than an active
# resource. Copy ingress.example.yaml to ingress.yaml, replace its ACM ARN and
# host, then add `- ingress.yaml` above only after the certificate is issued.
