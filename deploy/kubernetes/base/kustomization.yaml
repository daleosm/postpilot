apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - migration-job.yaml
  - deployment.yaml
  - pod-disruption-budget.yaml
  - service.yaml

labels:
  - pairs:
      app.kubernetes.io/name: postpilot
      app.kubernetes.io/part-of: postpilot
