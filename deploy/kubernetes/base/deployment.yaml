apiVersion: apps/v1
kind: Deployment
metadata:
  name: postpilot
  namespace: postpilot
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: postpilot
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postpilot
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: postpilot
          image: 778183943831.dkr.ecr.eu-west-1.amazonaws.com/postpilot:bdb21047b803cccccf6b1c06bf7f38181bc53a56-29852452465
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3000
          envFrom:
            - secretRef:
                name: postpilot-secrets
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
