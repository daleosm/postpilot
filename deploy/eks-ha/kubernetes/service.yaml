apiVersion: v1
kind: Service
metadata:
  name: postpilot
  namespace: postpilot
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postpilot
  ports:
    - name: http
      port: 80
      targetPort: http
