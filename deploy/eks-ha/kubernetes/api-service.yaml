apiVersion: v1
kind: Service
metadata:
  name: postpilot-api
  namespace: postpilot
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postpilot-api
  ports:
    - name: http
      port: 80
      targetPort: http
