import { Building2, ContactRound, ReceiptText } from "lucide-react";
import { redirect } from "next/navigation";
import { getActiveOrganizationContext } from "@/lib/organizations";
import { can } from "@/lib/permissions";
import { getCrmData } from "@/server/data";

export default async function CrmPage() { if (!(await can("manage_shows")) && !(await can("manage_budget"))) redirect("/"); const context = await getActiveOrganizationContext(); const data = context?.organization ? await getCrmData(context.organization.organizationId) : { companies: [], contacts: [], purchaseOrders: [] }; return <div className="space-y-5"><header><p className="text-xs font-medium uppercase tracking-[.12em] text-[#7c827f]">Commercial contacts · {context?.organization?.organizationName ?? "Workspace"}</p><h1 className="mt-2 text-[27px] font-semibold tracking-[-.045em] text-[#202524]">Clients & vendors</h1><p className="mt-1 text-sm text-[#747977]">Client companies, vendor contacts, payment terms, and purchase orders.</p></header><section className="grid gap-3 sm:grid-cols-3"><Metric icon={<Building2 size={16}/>} label="Companies" value={data.companies.length}/><Metric icon={<ContactRound size={16}/>} label="Contacts" value={data.contacts.length}/><Metric icon={<ReceiptText size={16}/>} label="Open POs" value={data.purchaseOrders.filter((po) => po.status === "open").length}/></section><section className="panel overflow-hidden"><div className="border-b px-5 py-4 text-sm font-semibold">Companies & payment terms</div>{data.companies.map((company) => <div key={company.id} className="flex justify-between gap-3 border-b px-5 py-3 text-sm"><span><b>{company.name}</b><small className="ml-2 capitalize text-[#858a87]">{company.type.replaceAll("_", " ")}</small></span><span className="text-xs text-[#68716d]">{company.paymentTermsDays ? `Net ${company.paymentTermsDays}` : "Terms not set"}</span></div>)}{!data.companies.length && <p className="px-5 py-8 text-sm text-[#858a87]">No companies yet.</p>}</section></div>; }
function Metric({ icon, label, value }: { icon: React.ReactNode; label: string; value: number }) { return <div className="panel flex items-center gap-3 p-4"><span className="text-[#58746b]">{icon}</span><div><p className="text-lg font-semibold">{value}</p><p className="text-xs text-[#7b817e]">{label}</p></div></div>; }
