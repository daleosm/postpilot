# PostPilot deliberately uses a small, public-subnet EKS demonstration stack
# without a NAT gateway, Multi-AZ database, or paid observability add-ons.
# These documented exceptions keep Checkov strict for future changes while
# avoiding false expectations that this cost-conscious demo is production HA.
skip-check:
  # AWS-managed ECR encryption is adequate for public demo container images.
  - CKV_AWS_136
  # Nodes are deliberately in public subnets so the demo works without NAT.
  - CKV_AWS_130
  # The EKS API is public but CIDR-restricted to the operator's address.
  - CKV_AWS_39
  # KMS envelope encryption and full EKS control-plane logs add recurring cost.
  - CKV_AWS_58
  - CKV_AWS_37
  # RDS is a low-cost, single-AZ demo database. These are production hardening
  # controls to enable before handling real customer data.
  - CKV_AWS_118
  - CKV_AWS_353
  - CKV_AWS_293
  - CKV_AWS_157
  - CKV_AWS_161
  - CKV_AWS_129
  - CKV_AWS_226
  - CKV_AWS_16
  - CKV2_AWS_30
  - CKV2_AWS_60
  # VPC flow logging adds recurring CloudWatch costs for this learning stack.
  - CKV2_AWS_11
  # Application error logs retain for thirty days by design. Raising this to a
  # year is opt-in through application_log_retention_days for real production
  # deployments with longer audit requirements.
  - CKV_AWS_338
  # AWS KMS key policies must use Resource "*". The only service-principal
  # statement is constrained to the PostPilot CloudWatch log-group encryption
  # context, and the account-administration statement is AWS's standard key
  # policy bootstrap requirement.
  - CKV_AWS_109
  - CKV_AWS_111
  - CKV_AWS_356
  # Default-group restriction is managed by the AWS account baseline.
  - CKV2_AWS_12
  # PostgreSQL egress and rule descriptions remain unchanged in the existing
  # minimal network design; revisit before any production rollout.
  - CKV_AWS_23
  - CKV_AWS_382
